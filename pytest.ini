[pytest]
testpaths = tests
pythonpath = . tests
addopts = -q --strict-markers --timeout=60
# Several tests drive real subprocesses. Without a ceiling a broken one hangs
# the whole run instead of failing with a traceback that names it.
timeout_method = thread
filterwarnings =
    ignore::DeprecationWarning
